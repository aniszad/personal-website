# Portfolio Website

## Favicon

- The site favicon is located at `/app/icon.png`.
- Keep it as a square PNG (`>=48x48`) when replacing it.
- The app metadata links it from the site root as `/icon.png` for search engine discovery.
